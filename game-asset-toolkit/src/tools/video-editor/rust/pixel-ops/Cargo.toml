[package]
name = "video-pixel-ops"
version = "1.0.0"
edition = "2021"
publish = false
description = "Local RGBA color transforms for Game Asset Toolkit"

[lib]
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true

